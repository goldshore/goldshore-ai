---
import BaseLayout from './BaseLayout.astro';
const { title, description, canonicalPath, ogTitle, ogDescription } = Astro.props;
---
<BaseLayout {title} {description} {canonicalPath} {ogTitle} {ogDescription}>
  <slot />
</BaseLayout>
