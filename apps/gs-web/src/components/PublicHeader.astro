---
import { CANONICAL_ADMIN_DASHBOARD_URL } from '../utils/admin-access';
import { primaryNavLinks } from '../config/navigation';

const { currentPath = Astro.url.pathname } = Astro.props;
const normalizePath = (path: string) => {
  const normalized = path.startsWith('/') ? path : `/${path}`;
  return normalized === '/' ? '/' : `${normalized.replace(/\/+$/, '')}/`;
};
const activePath = normalizePath(currentPath);
const isCurrent = (href: string) => {
  const normalizedHref = normalizePath(href);
  return normalizedHref === '/'
    ? activePath === '/'
    : activePath === normalizedHref || activePath.startsWith(normalizedHref);
};
---

<header class="topbar" data-menu-open="false" data-theme-header>
  <a class="brand brand-text" href="/" aria-label="GoldShore home">
    <img src="/assets/penrose-logo.png" alt="GS Labs" class="brand-logo" />
    <span class="brand-wordmark brand-text-label"><span>GOLD</span><span class="brand-shore">SHORE</span></span>
  </a>
  <button id="nav-toggle" class="nav-toggle" type="button" aria-expanded="false" aria-controls="main-nav" aria-label="Toggle menu"><span></span><span></span><span></span></button>
  <nav id="main-nav" class="main-nav" aria-label="Primary navigation">
    {primaryNavLinks.map((link) => <a href={link.href} aria-current={isCurrent(link.href) ? 'page' : undefined}>{link.label}</a>)}
    <a href={CANONICAL_ADMIN_DASHBOARD_URL} class="nav-login" data-admin-access-link>Log in</a>
    <a href="/contact/" class="nav-cta">Request Briefing</a>
  </nav>
</header>
