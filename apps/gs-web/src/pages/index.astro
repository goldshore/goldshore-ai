---
import '../styles/home-theme.css';

export const prerender = true;

const services  = ['Digital Strategy','AI Implementation','Systems Integration','Design & Development','Enterprise Consulting'];
const sectors   = ['Financial Services','Operations & Logistics','Media & Communications','Healthcare Systems','Public Sector'];
const platform  = [['Risk Radar','Live'],['Financial Signals','Live'],['Workflow Engine','Live'],['Sentiment Engine','Beta'],['AI Oracle','Soon']];
const metrics   = [['Uptime','99.98%','30-day avg'],['Signal Events','4.8B','Per month'],['Latency P99','<45ms','Gateway'],['Deploys','48/d','Verified']];
const sectorCards = [['Financial Services','Risk · Signals · Trading'],['Operations & Logistics','Workflow · Automation'],['Media & Comms','Content · Analytics'],['Healthcare Systems','Compliance · AI'],['Public Sector','Gov · Infrastructure'],['Enterprise Tech','Dev · Design · Systems']];
const telemetry = [['Platform Uptime','99.98%','30-day rolling'],['Signal Events/mo','4.8B','Validated telemetry'],['Operator Latency','<45ms','P99 gateway'],['Deploy Cadence','48/day','Verified releases']];
const capabilities = [
  ['Risk Intelligence & Financial Signals','Continuous surveillance across positions, portfolios, and market conditions. Anomaly detection, threshold alerting, operator-in-loop review — built for institutions that cannot afford surprises.',['Risk Radar','Sentiment Engine','Market Feeds','Alpaca','Bloomberg']],
  ['AI Systems & Workflow Automation',"End-to-end workflow design and execution with AI at every node. Queue-backed, auditable, with fallback paths and operator checkpoints. We don't deploy AI that cannot explain itself.",['Gemini','GPT-4','Workflow Engine','Cloudflare AI']],
  ['Digital Strategy & Systems Consulting','Architecture planning, vendor evaluation, capability mapping — the upstream work that makes everything downstream coherent. Strategy that ships, not slides.',['Strategy','Architecture','Due Diligence','Roadmapping']],
  ['Design, Development & Product','Full-cycle product and software delivery. Brand identity, design systems, Astro frontends, Cloudflare Workers, React Native — shipped on time, built to last.',['Astro','React Native','CF Workers','Design Systems']],
  ['Resilient Infrastructure & Observability','Production-grade infrastructure with full observability. Cloudflare Access, D1 databases, KV audit trails, R2 storage — secure by default, traceable at every layer.',['Cloudflare','D1','Hono','JWKS Auth']],
];
const stackRows = [['gs-gateway','Ingress, CORS, auth, integration routing','Mar 29','Live'],['gs-agent','Autonomous processor, queue consumer','Mar 19','Live'],['gs-api','Core API, data access layer','Mar 22','Live'],['gs-mail','Transactional mail service','Mar 29','Live'],['gs-control','Control plane, operator interface','Mar 22','Live']];
const anomalies = [['ALGO-DRIFT — Position delta exceeds 3σ','High','00:42'],['SENTIMENT — NVDA neg. velocity +18%','Med','01:14'],['LATENCY — GW P99 at 42ms','Low','02:07'],['CORRELATION — BTC/SPX decoupling','Med','03:55']];
const tickers   = [['SPY','S&P 500 ETF','521.84','+1.24%'],['NVDA','Nvidia Corp','887.22','+2.87%'],['BTC','Bitcoin','68,420','−0.43%'],['GLD','Gold ETF','2,391','+0.61%'],['TLT','20Y Treasury','93.14','−0.18%'],['VIX','Volatility Idx','13.82','−3.20%']];
const principles = [['01','Sector-Agnostic, Systems-First',"We don't specialize in one vertical — we specialize in the architecture that makes any vertical work. Finance, operations, media, healthcare: the discipline is identical."],['02','AI as Infrastructure, Not Feature','Intelligence embedded at the routing layer, not bolted on at the end. Every workflow treats AI as a first-class component with audit trails to match.'],['03','Consulting That Ships','Strategy without delivery is overhead. We provide the advisory and we build the thing — one team, one contract, full accountability from brief to production.']];
const marquee   = ['Risk Intelligence','Financial Signal Analysis','Workflow Automation','AI Implementation','Digital Strategy','Systems Integration','Design & Development','Enterprise Consulting','Sentiment Analysis','Applied Intelligence','Resilient Infrastructure','Cross-Sector Deployment'];
---
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Gold Shore Labs — applied intelligence, digital systems, and consulting." />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://goldshore.ai/" />
  <meta property="og:title" content="Gold Shore Labs | Applied Intelligence" />
  <meta property="og:description" content="Applied intelligence for institutions that need clarity under pressure." />
  <title>Gold Shore Labs | Applied Intelligence</title>
  <meta name="google-adsense-account" content="ca-pub-5105781580031290" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&family=DM+Mono:wght@300;400;500&display=swap" onload="this.onload=null;this.rel='stylesheet'" />
  <noscript><link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&family=DM+Mono:wght@300;400;500&display=swap" rel="stylesheet" /></noscript>
</head>
<body class="gs-home">

<a class="skip-nav" href="#main-content">Skip to main content</a>
<canvas id="starfield" aria-hidden="true"></canvas>

<header class="topbar">
  <a class="brand" href="/" aria-label="GoldShore home">
    <span class="brand-wordmark">GOLDSHORE</span>
  </a>
  <button class="nav-toggle" id="nav-toggle" aria-label="Toggle menu" aria-expanded="false">
    <span></span><span></span><span></span>
  </button>
  <nav id="main-nav">
    <a href="/platform/">Platform</a>
    <a href="/risk-radar/">Risk Radar</a>
    <a href="/services/">Services</a>
    <a href="/developer/">Developer</a>
    <a href="/about/">About</a>
    <button type="button" id="open-access-modal" class="nav-login">Access →</button>
    <a class="nav-cta" href="#engage">Request Briefing</a>
  </nav>
</header>

<main id="main-content">

<!-- ── Hero ────────────────────────────────────────────────────────────────────────── -->
<section class="hero" id="platform">
  <div class="hero-copy">
    <p class="kicker">Gold Shore Labs · Est. 2022 · New York</p>
    <h1>
      <span class="solid">Where</span>
      <span class="outlined">Strategy</span>
      <span class="accent" data-text="Operates.">Operates.</span>
    </h1>
    <p class="lead">Applied intelligence for institutions that need clarity under pressure. We build the software, run the strategy, and deploy the AI — cross-sector, full-stack, end to end.</p>
    <div class="actions">
      <a class="btn primary magnetic" href="#engage">Request Briefing</a>
      <a class="btn magnetic" href="#services">View Platform</a>
      <a class="text-link" href="/developer">Developer Hub →</a>
    </div>
    <div class="metrics">
      {metrics.map(([label,value,sub]) => (
        <div><span>{label}</span><strong>{value}</strong><em>{sub}</em></div>
      ))}
    </div>
  </div>
  <aside class="hero-panel">
    <p class="panel-title">Applied Intelligence · Digital Systems · Consulting</p>
    <div class="status-list">
      {platform.map(([name,state]) => (
        <div><span>{name}</span><b class={state.toLowerCase()}>{state}</b></div>
      ))}
    </div>
    <div class="panel-columns">
      <div><p class="panel-col-heading">Services</p>{services.map((s) => <p>{s}</p>)}</div>
      <div><p class="panel-col-heading">Sectors</p>{sectors.map((s) => <p>{s}</p>)}</div>
    </div>
    <p class="operational"><span></span>Operational</p>
  </aside>
</section>

<!-- ── Sector surface ────────────────────────────────────────────────────────── -->
<section class="surface" data-reveal>
  <div class="section-head">
    <div>
      <p class="kicker">Cross-Sector Deployment Surface</p>
      <h2>5 active sectors</h2>
    </div>
  </div>
  <div class="sector-grid">
    {sectorCards.map(([name,desc]) => (
      <article><h3>{name}</h3><p>{desc}</p></article>
    ))}
  </div>
</section>

<!-- ── Telemetry ─────────────────────────────────────────────────────────────── -->
<section class="telemetry" data-reveal>
  <div class="section-head">
    <div>
      <p class="kicker">Platform Telemetry — Live</p>
      <h2><span class="live-dot"></span>Live</h2>
    </div>
  </div>
  <div class="telemetry-grid">
    {telemetry.map(([label,value,sub]) => (
      <article><span>{label}</span><strong>{value}</strong><em>{sub}</em></article>
    ))}
  </div>
</section>

<!-- ── Marquee ─────────────────────────────────────────────────────────────────── -->
<div class="marquee">
  <div>{[...marquee,...marquee].map((item) => <span>{item}</span>)}</div>
</div>

<!-- ── Capabilities ─────────────────────────────────────────────────────────────────── -->
<section class="capabilities" data-reveal id="services">
  <div class="section-head large">
    <div>
      <p class="kicker">01 — Capabilities</p>
      <h2>Cross-<br />Sector.<br />Full-Stack.</h2>
    </div>
    <p class="section-desc">We operate at the intersection of digital consulting, product engineering, and applied AI — building systems that hold under pressure across every sector we touch.</p>
  </div>
  <div class="cap-list">
    {capabilities.map(([title,body,tags],i) => (
      <article>
        <div class="cap-index">{String(i + 1).padStart(2,'0')}</div>
        <div>
          <h3>{title}<span>↗</span></h3>
          <p>{body}</p>
          <div class="tags">{tags.map((tag) => <b>{tag}</b>)}</div>
        </div>
      </article>
    ))}
  </div>
</section>

<!-- ── Under the Hood ────────────────────────────────────────────────────────────────── -->
<section class="underhood" data-reveal id="developer">
  <div class="section-head">
    <div>
      <p class="kicker">02 — Under the Hood</p>
      <h2>Systems that<br />Explain<br />Themselves.</h2>
    </div>
  </div>
  <div class="code-stack">
    <pre><span style="font-family:var(--font-mono);font-size:0.6rem;letter-spacing:0.06em;text-transform:uppercase;color:var(--t3);">packages/auth/verify.ts</span><code>{`
export async function verifyAccess(req, env) {
  const identity = await validateRequest(req, env);
  if (!identity.authorized) return { authorized: false };
  return { authorized: true, email: identity.email };
}`}</code></pre>
    <aside class="stack-card">
      <h3>Production Stack — goldshore.ai</h3>
      {stackRows.map(([name,desc,date,state]) => (
        <div class="stack-row">
          <div><strong>{name}</strong><p>{desc}</p></div>
          <time>{date}</time>
          <b>{state}</b>
        </div>
      ))}
      <div class="infra">
        <h4>Infrastructure Resources</h4>
        <p><b>R2</b> gs-assets · gs-telemetry · user-uploads</p>
        <p><b>D1</b> gs_platform_db · gs_audit_db</p>
        <p><b>Stack</b> Hono · pnpm · wrangler</p>
      </div>
    </aside>
  </div>
</section>

<!-- ── Risk Radar ─────────────────────────────────────────────────────────────────── -->
<section class="risk" data-reveal id="risk-radar">
  <div class="risk-copy">
    <p class="kicker">03 — Risk Radar</p>
    <h2>Always<br />Sweeping.<br />Never Silent.</h2>
    <p>Continuous surveillance across monitored positions and portfolios. Anomalies surface in real time — classified by severity, routed to the right operator, with a full audit trace from detection to resolution.</p>
  </div>
  <div class="risk-board">
    <div class="risk-metrics">
      <span>Sweep Interval<b>2.4s</b></span>
      <span>Active Anomalies<b>5 Live</b></span>
      <span>Decision Mode<b>Human+AI</b></span>
      <span>Review Posture<b>High Trust</b></span>
    </div>
    {anomalies.map(([name,sev,time]) => (
      <div class="anomaly">
        <strong>{name}</strong>
        <b class={sev.toLowerCase()}>{sev}</b>
        <time>{time}</time>
      </div>
    ))}
  </div>
</section>

<!-- ── Financial Signals ──────────────────────────────────────────────────────────────── -->
<section class="signals" data-reveal id="signals">
  <div class="section-head">
    <div>
      <p class="kicker">04 — Financial Signals</p>
      <h2>Signal,<br />Not Noise.</h2>
    </div>
    <a class="text-link" href="/apps/risk-radar">Full Signal Feed →</a>
  </div>
  <div class="signals-body">
    <div class="ticker-col">
      {tickers.map(([sym,name,price,change]) => (
        <div class="ticker-item">
          <span class="ticker-sym">{sym}</span>
          <span class="ticker-name">{name}</span>
          <span class="ticker-price">{price}</span>
          <span class={`ticker-chg ${change.startsWith('−') ? 'down' : 'up'}`}>{change}</span>
        </div>
      ))}
    </div>
    <div class="chart-panel">
      <div class="chart-panel-head">
        <div>
          <h3>SPY</h3>
          <p>SPDR S&amp;P 500 ETF · NYSE Arca · 4h</p>
        </div>
      </div>
      <p class="chart-big-price">$521.84 <span>▲ +6.37 (+1.24%)</span></p>
      <div class="chart-line"></div>
      <div class="chart-stats">
        <span>RSI 14<b>58.4</b></span>
        <span>MACD<b>+2.1</b></span>
        <span>Vol 10d<b>12.3%</b></span>
        <span>Beta<b>1.00</b></span>
        <span>Sentiment<b>Bullish</b></span>
        <span>Signal<b>Watch</b></span>
      </div>
    </div>
  </div>
</section>

<!-- ── Approach / Principles ──────────────────────────────────────────────────────────────── -->
<section class="approach" id="about" data-reveal>
  <div class="section-head">
    <div>
      <p class="kicker">05 — Approach</p>
      <h2>How We Work.</h2>
    </div>
  </div>
  <div class="inner">
    {principles.map(([num,title,body]) => (
      <div class="approach-item">
        <div class="approach-num">{num}</div>
        <h3>{title}</h3>
        <p>{body}</p>
      </div>
    ))}
  </div>
</section>

<!-- ── Engage ───────────────────────────────────────────────────────────────────────────── -->
<section class="engage" data-reveal id="engage">
  <div>
    <p class="kicker">06 — Engage</p>
    <h2>Ready to<br />Operationalize.</h2>
    <p>Whether you need a strategic briefing, a technical audit, a delivered product, or an embedded AI system — the conversation starts here.</p>
    <div class="actions">
      <a class="btn primary magnetic" href="mailto:hello@goldshore.ai?subject=Gold%20Shore%20Briefing%20Request">Request Briefing →</a>
      <a class="btn magnetic" href="/developer">Developer Hub →</a>
    </div>
  </div>
  <form class="quick-form" id="quick-form" method="POST" action="/api/contact">
    <h3>Quick Inquiry</h3>
    <input type="hidden" name="formType" value="general-inquiry" />
    <input type="hidden" name="redirectTo" value="/thank-you" />
    <input type="hidden" name="formStartedAt" id="qs-form-started" value="" />
    <input type="hidden" name="dedupeKey" id="qs-dedupe-key" value="" />
    <div class="gs-honeypot"><input name="companyWebsite" tabindex="-1" autocomplete="off" /></div>
    <div class="form-row">
      <label>Name<input name="name" required placeholder="Your name" /></label>
      <label>Organisation<input name="company" placeholder="Firm or company" /></label>
    </div>
    <label>Work Email<input name="email" type="email" required placeholder="you@firm.com" /></label>
    <label>Interest
      <select name="inquiry" required>
        <option value="">Select area of interest</option>
        <option value="general-inquiry">General inquiry</option>
        <option value="strategy-call">Strategy Call</option>
        <option value="project-scope">Project scoping</option>
        <option value="support">Support</option>
      </select>
    </label>
    <label>Message
      <textarea name="message" id="qs-message" required rows="4" placeholder="Tell us about your inquiry..."></textarea>
    </label>
    <div id="qs-form-status" class="form-status" aria-live="polite"></div>
    <button type="submit" class="btn-form" id="qs-submit">Submit Request →</button>
  </form>
</section>

</main>

<footer class="site-footer">
  <div>
    <h2>GoldShore</h2>
    <p>Applied Intelligence · Digital Systems · Consulting</p>
    <p>Institutional AI systems, risk tooling, and resilient operator infrastructure. Cross-sector. Full-stack. Built to last.</p>
  </div>
  <div>
    <h3>Platform</h3>
    <p><a href="/platform/">Overview</a></p>
    <p><a href="/risk-radar/">Risk Radar</a></p>
    <p><a href="/platform/financial-signals">Financial Signals</a></p>
    <p><a href="/platform/workflow-engine">Workflow Engine</a></p>
    <p><a href="/platform/sentinel">Sentinel</a></p>
    <p><a href="/platform/ai-oracle">AI Oracle</a></p>
  </div>
  <div>
    <h3>Services</h3>
    <p><a href="/services/">Overview</a></p>
    <p><a href="/services/digital-strategy">Digital Strategy</a></p>
    <p><a href="/services/ai-implementation">AI Implementation</a></p>
    <p><a href="/services/systems-integration">Systems Integration</a></p>
    <p><a href="/services/design-dev">Design &amp; Development</a></p>
    <p><a href="/services/consulting">Enterprise Consulting</a></p>
  </div>
  <div>
    <h3>Developer</h3>
    <p><a href="/developer/">Hub</a></p>
    <p><a href="/developer/api/">API</a></p>
    <p><a href="/developer/docs/">Docs</a></p>
    <p><a href="/developer/sdk">SDK</a></p>
    <p><a href="/developer/mcp">MCP</a></p>
  </div>
  <div>
    <h3>Company</h3>
    <p><a href="/about/">About</a></p>
    <p><a href="/team/">Team</a></p>
    <p><a href="/status/">Status</a></p>
    <p><a href="/contact/">Contact</a></p>
    <p><a href="/legal/privacy">Privacy</a></p>
    <p><a href="/legal/terms">Terms</a></p>
  </div>
  <small>© 2026 Gold Shore Labs. All rights reserved. 40°42′N · 074°00′W · goldshore.ai · <a href="/legal/privacy">Privacy</a> · <a href="/legal/">Security</a> · <a href="/legal/terms">Terms</a></small>
</footer>

<!-- ── Access Modal ────────────────────────────────────────────────────────────────── -->
<dialog id="access-modal" class="access-modal" aria-labelledby="access-modal-title">
  <div class="modal-inner">
    <button class="modal-close" aria-label="Close">✕</button>
    <h2 id="access-modal-title">Access Goldshore</h2>
    <p class="modal-lead">Select a destination to continue.</p>
    <div class="modal-grid">
      <a href="https://admin.goldshore.ai" class="modal-card" data-admin-access-link>
        <span class="modal-icon">⬡</span>
        <strong>Admin</strong>
        <span>Operator dashboard</span>
      </a>
      <a href="/developer/mcp" class="modal-card">
        <span class="modal-icon">⬢</span>
        <strong>MCP</strong>
        <span>Model Context Protocol</span>
      </a>
      <a href="https://api.goldshore.ai" class="modal-card">
        <span class="modal-icon">◉</span>
        <strong>API</strong>
        <span>Developer access</span>
      </a>
    </div>
    <p class="modal-contact">Need access? <a href="/contact/">Contact us →</a></p>
  </div>
</dialog>
<script is:inline>
/* ── Critical interactivity (runs immediately after DOM parse) ─────────────────────── */

/* Scroll reveal */
document.documentElement.classList.add('js-ready');
const io = new IntersectionObserver(entries => {
  entries.forEach(e => {
    if (e.isIntersecting) { e.target.classList.add('is-visible'); io.unobserve(e.target); }
  });
}, { threshold: 0.1 });
document.querySelectorAll('[data-reveal], .sector-grid article, .cap-list article, .telemetry-grid article')
  .forEach(el => io.observe(el));

/* Hamburger nav toggle */
const navToggle = document.getElementById('nav-toggle');
const mainNav   = document.getElementById('main-nav');
navToggle?.addEventListener('click', () => {
  const open = mainNav.classList.toggle('nav-open');
  navToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
});

/* Access modal */
const accessModal = document.getElementById('access-modal');
document.getElementById('open-access-modal')?.addEventListener('click', () => accessModal?.showModal());
document.querySelector('.modal-close')?.addEventListener('click', () => accessModal?.close());
accessModal?.addEventListener('click', e => { if (e.target === accessModal) accessModal.close(); });

/* ── Quick engage form ───────────────────────────────────────────────────────────────────────── */
const qsForm    = document.getElementById('quick-form');
const qsStarted = document.getElementById('qs-form-started');
const qsStatus  = document.getElementById('qs-form-status');
const qsSubmit  = document.getElementById('qs-submit');

if (qsStarted) qsStarted.value = Date.now().toString();

qsForm?.addEventListener('submit', async e => {
  e.preventDefault();
  const fd  = new FormData(qsForm);
  const msg = (fd.get('message') || '').toString().trim();
  if (msg.length < 20) {
    qsStatus.textContent = 'Message must be at least 20 characters.';
    qsStatus.className = 'form-status';
    return;
  }
  qsStatus.textContent = '';
  qsSubmit.disabled = true;
  qsSubmit.textContent = 'Sending…';
  try {
    const raw = `${fd.get('formType')}|${fd.get('name')}|${fd.get('email')}|${fd.get('inquiry')}|${msg}`;
    const buf = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(raw));
    document.getElementById('qs-dedupe-key').value = Array.from(new Uint8Array(buf)).map(b => b.toString(16).padStart(2,'0')).join('');
    const res  = await fetch('/api/contact', {
      method: 'POST',
      headers: { 'x-gs-request-mode': 'spa', 'Accept': 'application/json' },
      body: new FormData(qsForm),
    });
    if (res.ok) {
      window.location.href = '/thank-you';
    } else {
      const data = await res.json().catch(() => ({}));
      qsStatus.textContent = data.error || 'Something went wrong. Please try again.';
      qsStatus.className = 'form-status';
      qsSubmit.disabled = false;
      qsSubmit.textContent = 'Submit Request →';
    }
  } catch {
    qsStatus.textContent = 'Network error. Please try again.';
    qsStatus.className = 'form-status';
    qsSubmit.disabled = false;
    qsSubmit.textContent = 'Submit Request →';
  }
});

/* ── Deferred decorative enhancements (after page load) ─────────────────────────────────── */
window.addEventListener('load', function () {

  /* Starfield canvas animation */
  (function () {
    const canvas = document.getElementById('starfield');
    if (!canvas || window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    const ctx = canvas.getContext('2d');
    let stars = [];
    function resize() {
      canvas.width  = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    function init() {
      stars = Array.from({ length: 160 }, () => ({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        r: Math.random() * 1.2,
        a: Math.random() * Math.PI * 2,
        s: 0.0002 + Math.random() * 0.0003,
      }));
    }
    function draw() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      stars.forEach(s => {
        s.a += s.s;
        const alpha = 0.2 + 0.5 * Math.abs(Math.sin(s.a));
        ctx.beginPath();
        ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(236,234,228,${alpha})`;
        ctx.fill();
      });
      requestAnimationFrame(draw);
    }
    resize(); init(); draw();
    window.addEventListener('resize', () => { resize(); init(); }, { passive: true });
  })();

  /* Magnetic buttons */
  if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    document.querySelectorAll('.magnetic').forEach(el => {
      el.addEventListener('pointermove', e => {
        const r = el.getBoundingClientRect();
        el.style.setProperty('--mx', `${(e.clientX - r.left - r.width  / 2) * 0.12}px`);
        el.style.setProperty('--my', `${(e.clientY - r.top  - r.height / 2) * 0.12}px`);
      });
      el.addEventListener('pointerleave', () => {
        el.style.setProperty('--mx', '0px');
        el.style.setProperty('--my', '0px');
      });
    });
  }

}, { once: true, passive: true });
</script>

</body>
</html>