---
import AdminLayout from '../../layouts/AdminLayout.astro'; import AdminWorkspaceManager from '../../components/AdminWorkspaceManager.astro';
if (!Astro.locals.adminSession?.isAuthenticated) return Astro.redirect('/login');
---
<AdminLayout title="API Workflows" requiredPermission="system:read"><AdminWorkspaceManager type="workflows" title="API Workflows" description="Document production workflow triggers, routes, retries, ownership, and runbooks." fields={['owner','status','trigger','api_route','retry_policy']}/></AdminLayout>
