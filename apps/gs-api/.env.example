# Secret values are intentionally omitted. An authorized operator must enter them
# directly in the Cloudflare dashboard; see secret-contract.json for exact names.

# Non-secret Cloudflare AI Gateway compatibility endpoint.
CF_GATEWAY_URL=https://gateway.ai.cloudflare.com/v1/<account-id>/<gateway-id>/compat
